<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>CognitiveDrive -
        {% block title %}Bienvenue{% endblock %}
    </title>
    <link href="/CSS/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.2/css/all.css"
     integrity="sha384-oS3vJWv+0UjzBfQzYUhtDYW+Pj2yciDJxpsK1OYPAYjqT085Qq/1cq5FLXAZQ7Ay" crossorigin="anonymous">
    <link href="/CSS/index.css" rel="stylesheet">
    <link rel="icon" href="Image/favicon.ico" />
    <script src="https://www.google.com/recaptcha/api.js" async defer></script>
    <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
    {% block stylesheets %}{% endblock %}
    <style>
        .card{
            margin-top: 10%;
        }
     </style>
</head>

<body>
    <header>
        <nav class="navbar navbar-expand-lg navbar-dark bg-light">
            <a class="navbar-brand" href="#">CognitiveDrive</a>
            <button aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation"
                class="navbar-toggler" data-target="#navbarColor01" data-toggle="collapse" type="button">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav mr-auto">
                    <li class="nav-item"><a class="nav-link" href="{{path('admin_main_psycho')}}"><i class="fas fa-users"></i> Les utilisateurs</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{path('admin_forum_index')}}"><i class="fas fa-comments"></i> Forum</a></li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('admin_main_patient')}}"><i class="fas fa-folder"></i> Les patients</a>
                    </li>     
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('admin_dashboard')}}"><i class="fas fa-chart-line"></i> Dashboard</a>
                    </li>
                    <li class="nav-item"><a class="nav-link" href="{{path('admin_deconnexion_user')}}">Deconnexion</a></li>
                </ul>
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item">
                        <a href="{{path('acceuil')}}" target = "_blank" class="nav-link"><i class="fas fa-eye"></i> Voir le site</a>
                    </li>
                </ul>
            </div>

        </nav>
        {% for label,messages in app.flashes %}
            <div class="container">
                <div class="alert alert-{{label}} text-center">
                {% for message in messages %}
                    <p>
                        {{message|raw}}
                    </p>
                {% endfor %}
                </div>
            </div>
        {% endfor %}
    </header>
    {% block body %}{% endblock %}
    <footer class="mt-5 bg-secondary py-5 text-center">
        <p><a class="btn btn-secondary" href="{{path('propos')}}">A propos</a></p>
        <p><a class="btn btn-secondary" href="{{path('rgpd')}}">Protection de vos données</a></p>
    </footer>
    <script src="/JS/jquery.min.js"></script>
    <script src="/JS/bootstrap.min.js"></script>
    {% block javascripts %}{% endblock %}
</body>

</html>